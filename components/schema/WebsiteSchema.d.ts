declare module '@/components/schemas/WebsiteSchema' {
  import { FC } from 'react';
  
  interface WebsiteSchemaProps {
    // Add any props here if needed
  }
  
  const WebsiteSchema: FC<WebsiteSchemaProps>;
  export default WebsiteSchema;
}
