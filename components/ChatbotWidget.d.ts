declare module '@/components/ChatbotWidget' {
  import { FC } from 'react';
  
  interface ChatbotWidgetProps {
    // Add any props here if needed
  }
  
  const ChatbotWidget: FC<ChatbotWidgetProps>;
  export default ChatbotWidget;
}
