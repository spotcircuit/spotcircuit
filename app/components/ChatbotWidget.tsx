'use client';

import { useEffect } from 'react';

export function ChatbotWidget() {
  useEffect(() => {
    // Initialize chat widget here if needed
    return () => {
      // Cleanup
    };
  }, []);

  return null; // Or return your chat widget component
}
